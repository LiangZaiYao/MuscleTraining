<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.cqu.muscletraining">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".QuestionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChestActivity" />
        <activity android:name=".BackActivity" />
        <activity android:name=".AbdomenActivity" />
        <activity android:name=".UpChestActivity" />
        <activity android:name=".MiddleChestActivity" />
        <activity android:name=".DownChestActivity"></activity>
    </application>

</manifest>